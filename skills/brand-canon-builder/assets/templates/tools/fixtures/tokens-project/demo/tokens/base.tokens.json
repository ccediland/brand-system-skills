{
  "base": {
    "color": {
      "$type": "color",
      "brand-1": {
        "$value": { "colorSpace": "oklch", "components": [0.62, 0.13, 255], "alpha": 1, "hex": "#5a7fb0" },
        "$description": "generic example ink — hex is the sRGB fallback and must NOT survive the string projection",
        "$extensions": { "brand": { "provenance": { "source": "declared-spec", "confidence": "hypothesis", "owner": "demo-owner", "freshness": "shipped" } } }
      },
      "veil": {
        "$value": { "colorSpace": "oklch", "components": [0.7, 0.1, 20], "alpha": 0.5, "hex": "#d99a94" },
        "$description": "generic example with alpha < 1 — must project as oklch(L C H / a)",
        "$extensions": { "brand": { "provenance": { "source": "declared-spec", "confidence": "hypothesis", "owner": "demo-owner", "freshness": "shipped" } } }
      }
    },
    "spacing": {
      "$type": "dimension",
      "1": { "$value": "0.25rem", "$description": "plain string — passes through untouched" }
    },
    "font-family": {
      "$type": "fontFamily",
      "body": { "$value": "\"Demo Face\", system-ui, sans-serif", "$description": "stack string — passes through untouched" }
    }
  }
}
