{
  "component": {
    "color": {
      "$type": "color",
      "button-bg": {
        "$value": "{semantic.color.primary}",
        "$description": "alias string — passes through untouched"
      }
    }
  }
}
