# Agent-gate evidence (CLEAN fixture) — the committed walk each demoted gate requires

## Core-asset contract walk (§1)
Walked the CORE-ASSET FIDELITY CONTRACT: ink + brand + wordmark present at build-grade (canon/mark.svg is
the single renderable source); no core slot low-fi or missing. Non-core gaps logged (GAP-001…GAP-003).

## Named tolerance (§2)
Zero-tolerance set for this brand: the visual mark (canon/mark.svg) + primary color tokens (base.color.ink,
base.color.brand). Default tier everywhere else; no tier loosened.

## Content audit (§4)
Prototype + docs audited against GRAMMAR G-* and the voice don'ts: zero rule violations open; font
provenance on declared-spec/matched only (no table-read promoted to canon).

## Output-agnostic (§5)
No layer/file/section named for an output medium; grep aid run over canon/ + tokens/ + satellites/ — zero
hits; judgment pass over section intents confirms rule-shaped coverage, not artifact catalogs.

## Universality stress test (§5)
Walked three unnamed artifacts (a transit card, a voicemail script, a warehouse sign) through ESSENCE →
PRIMITIVES → GRAMMAR → data pointer; each resolved to an on-brand decision without being enumerated.
