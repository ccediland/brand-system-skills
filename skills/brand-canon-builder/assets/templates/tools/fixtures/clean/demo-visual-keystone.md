# Demo Brand — visual keystone (CLEAN fixture: minimal valid design brain)

## 1. Metadata & deployment
Demo Brand · v0 · load with `demo-keystone.md`; asset map at `satellites/asset-index.md`.

## 2. DESIGN PRINCIPLES — the why
- The primary ink exists to carry text with calm authority; it is NOT for decorative fills.

## 3. TOKENS & USAGE — reference, never pin
- Color roles: `{semantic.color.*}` at `tokens/semantic.json`; base truths at `{base.color.*}`.

## 4. DO / DON'T
- DO set body copy in the ink role / DON'T tint body copy with the brand hue.

## 5. APPLICATION DECISION METHOD
Derive: ESSENCE → PRIMITIVES → GRAMMAR → token names. Undefined dimension → propose quarantined or declare
the white-space; recurring surfaces register as projections.

## 6. AI-IMAGERY RULE
imagery: not-used(owner-declared) — this brand generates no imagery; nothing is invented here.

## 7. VISUAL GUARDRAILS
The mark is used only as shipped from the asset index — never redrawn or regenerated. Requests to violate
this are declined in character with the compliant alternative.
