# Handoff fixtures — synthetic v6 contract instances

Three synthetic handoff blocks in the current contract shape (`brand-canon-scoper/references/handoff-format.md`,
v6: per-line `BRIEF{}` lineage · SIGNED BRIEF appendix (last block) · `WIRE-CHECK:` declared counts · sanctioned
wire vocabulary). They are the gate-acceptance material for every consumer of the contract: the Stage-0 parse,
the persisted-handoff pattern (`sources/handoff—<date>.md`), the wire verbatim-check (`tools/wire-check.mjs` —
all three PASS with exact recomputed counts), and the opt-out reconciliation gate (which asserts `Claude Design
component library: NO` ⇒ zero Claude Design artifacts). NOT emitted to clients (fixtures are never copied at
Stage 1). The BITING wire-check cases (untagged paraphrase · blanket not-used · fabricated verbatim · markers
without brief · vocabulary drift · all-empty CREATE N/A) live in `../wire-check/`.

- `full-kit-on—handoff.md` — Fixture Alpha · ANALYZE · FULL · kit YES. Exercises the whole carrier surface:
  one datum per SCOPER-STAMPABLE rung of the confidence ladder (`handoff-confirmed` is a builder-side
  inheritance label the scoper never stamps — it appears downstream, in the clean token fixture),
  `RATIFIED{by · how · date}` + all three `BRIEF{}` classes (verbatim · anchor · none — compiled/demoted),
  not-used rows with confirming quotes, `acquire:`/`fallback:` routes, a quarantined `PROPOSED` line, a
  treatment, posture, horizons, a complete dimension map, `WIRE-CHECK: markers:17 · verified:15 · demoted:2`.
- `demo-kit-off—handoff.md` — Fixture Beta · ANALYZE · v0/DEMO · **kit NO** (the opt-out variant the opt-out
  gate consumes) + a quarantined proposal. `WIRE-CHECK: markers:16 · verified:14 · demoted:2`.
- `spare-sonic—handoff.md` — Fixture Gamma · sonic-primary spare brand: massive `not-used(owner-declared)`
  (each row with its confirming quote), a non-visual primary-identity carrier (declared fidelity-blocking
  GAP — never a false-fail), `ingest:none-needed`, minimal palette. `WIRE-CHECK: markers:17 · verified:15 ·
  demoted:2`.

All three brands are synthetic placeholders (zero real brand content — the standing brand-scrub gate applies).
