# Demo Brand — canon index (CLEAN fixture)

A minimal but coherent emitted-canon slice that the gate passes (exit 0).

- The primary ink resolves to `{base.color.ink}` — a real token artifact.
- The primary action role is `{semantic.color.action}` — a real alias that resolves.
- The accent ink is **not yet ratified**: tracked as an open gap, so naming `{base.color.accent}` here resolves through GAP-001 rather than a token. (R4: a named value maps to a token artifact OR an open GAP-NNN.)
