Use the brand-canon-builder skill to build a brand canon in this repo.

BRAND: Fixture Genesis
OWNERS: demo-owner-g (all slots)   UNRESOLVED CONFLICTS: none
MODE: CREATE   BUILD-MODE: FULL
TARGET REPO: .

— MATERIAL MANIFEST (two tracks) —
  ASSETS (in-repo before build OR acquired by the builder per its declared route; checksummed):
    none
  CONSUMERS (live surfaces the brand ships today):
    none

— DIMENSION MAP (every dimension resolves; none skipped silently; scoper owns completeness) —
  color: tagged-gap · type: tagged-gap · imagery: tagged-gap · mark: tagged-gap
  applied-expression/social: tagged-gap
  consultation-surface: always-required

— HORIZONS (category-detected) —
  none detected yet — genuinely new brand

— POSTURE (guardrail layer) —
  profile:low-profile · visibility:low · audiences:to-be-defined · regulatory:none · stance:neutral · never-topics:none · refusal-style:plain decline

— CORE-ASSET FIDELITY CONTRACT (this brand's must-haves) —
  mark: GAP absent → fidelity-blocking (to be authored)
  NON-WAIVABLE even in v0/DEMO: the brand's PRIMARY-IDENTITY CARRIER(S) — visual mark (to-be-authored) · graphic-code

— GAPS (client-language; builder formalizes to GAP-NNN) —
  everything expressive — why: genuinely new brand, nothing exists yet · severity:MUST · provenance:handoff-deliberate · proposed: CREATE authors from the ratified WHY when it arrives

— OPTIONAL —
  print-collateral: not used
  Claude Design component library: NO
  existing-component-stack: none

NOTES: synthetic fixture handoff (Fixture Genesis) — the ALL-EMPTY CREATE wire: no ratification markers, no SIGNED BRIEF (nothing was elicited-and-signed yet), every expressive dimension a tagged gap. The acceptance case for the never-false-block guard: the wire-check self-declares N/A, the §7a mandatory set records the CREATE NOT-RUN instruction (authored master = source-of-record), the kit opt-out reconciles NO. Zero real brand content.
