# create-empty fixture — the ALL-EMPTY CREATE wire (never-false-block acceptance)

A minimal repo whose only content is a persisted all-empty `MODE: CREATE` handoff (no ratification
markers, no SIGNED BRIEF — nothing was elicited-and-signed yet; every expressive dimension `tagged-gap`;
directives filled: kit NO, stack none; NON-WAIVABLE carriers declared to-be-authored).

Run `node ../../../run-gates.mjs .` from this dir (or point run-gates here). The CREATE-sensitive rows
must resolve HONESTLY, never FAIL-for-lacking-what-CREATE-lacks:

| Row | Expected | Why |
|---|---|---|
| `wire verbatim-check` | **N/A (declared)** | no markers + no brief + vocabulary clean — the wire-check's own all-empty branch |
| `§7a mandatory set` | **NOT-RUN** with the CREATE instruction | carriers declared, no fidelity evidence yet — "diff against the AUTHORED master … or declare the medium N/A", never a false FAIL |
| `kit opt-out reconciliation` | **PASS** | slot NO + zero Claude Design artifacts |

The board's remaining FAIL rows (keystone/asset-index/agent-gates absent) are the generic
minimal-fixture structure rows — same as the custody/kit-off fixtures — and are NOT part of this
fixture's own class. Overall exit stays 1 (BLOCKED) for that reason; the acceptance is the three rows
above. `node ../../../wire-check.mjs .` alone: N/A (declared), exit 0.
