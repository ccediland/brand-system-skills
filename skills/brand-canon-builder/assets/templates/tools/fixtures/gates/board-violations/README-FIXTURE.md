# board-violations — seeded acceptance for run-gates.mjs (expect exit 1 / BLOCKED)

Seeded rows (8 FAIL): keystone structural FAIL (guardrail buried at §2 of 6 + battery.md present-but-EMPTY +
no expected-refusal contract) · keystone form-of-rule FAIL (THINK/DESIGN/SPEAK are bare adjective lists, no
when-X-then-Z shape, no visible tracked GAP marker) · §7a FAIL (audit/fidelity/fake-grain/scores.json
records verdict outside-tolerance with `pass:false` — a non-passing measurement can never feed ALL-GREEN) ·
all five demoted agent-gates FAIL (no audit/agent-gates.md).
audit-lint runs vacuous-PASS here (no tokens named); deny is N/A (no interim-scope surfaces). Self-test:
`node run-gates.mjs fixtures/gates/board-violations` → exit 1.
