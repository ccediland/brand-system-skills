# board-violations — seeded acceptance for run-gates.mjs (expect exit 1 / BLOCKED)

Seeded rows (12 FAIL): keystone structural FAIL (guardrail buried at §2 of 6 + battery.md present-but-EMPTY +
no expected-refusal contract) · keystone form-of-rule FAIL (THINK/DESIGN/SPEAK are bare adjective lists, no
when-X-then-Z shape, no visible tracked GAP marker) · §7a evidence FAIL (fake-grain: outside-tolerance and
UNTRACKED; hand-verdict: recorded `pass:true` disagrees with its own numbers — a hand-written verdict) ·
§7a mandatory set FAIL (the persisted handoff declares NON-WAIVABLE `visual mark (badmark)` + `graphic-code`
and neither was ever measured — ANALYZE mode) ·
all five demoted agent-gates FAIL (no audit/agent-gates.md) · audit-lint FAIL (a MISLOCATED projections.md
at the repo root — the R6a never-silent-skip seed) · visual keystone FAIL (bad-visual-keystone.md: buried
guardrails + a pinned `#hex` value + NO DO/DON'T pair — the body-only scan catches it — + empty imagery
section) · asset index FAIL (no satellites/asset-index.md — the resident set's third member is mandatory).
Deny is N/A (no interim-scope surfaces). Self-test:
`node run-gates.mjs fixtures/gates/board-violations` → exit 1.
