# Seeded brand — canon index (SEEDED-VIOLATION fixture)

This canon NAMES a value the build never materialized and never tracked:

- The display ink is `{base.color.ghost}` — there is no such token, and no GAP-NNN tracks it. (R4 VIOLATION: a named value mapping to neither a token artifact nor an open GAP.)
- The seasonal pattern is deferred under GAP-099. (R4 VIOLATION, dangling cross-ref: the ledger carries no GAP-099 row in ANY status — mis-numbered prose reads as tracked while nothing tracks it.)
