{
  "$comment": "Emits the .d.ts tree the /design-sync converter reads as each component's API contract. @types/react MUST resolve or React utility-type props collapse to `any` ([DTS_REACT]).",
  "compilerOptions": {
    "target": "ES2020",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "jsx": "react-jsx",
    "lib": ["ES2020", "DOM"],
    "declaration": true,
    "emitDeclarationOnly": true,
    "outDir": "dist",
    "strict": true,
    "skipLibCheck": true,
    "types": ["react"]
  },
  "include": ["src"]
}
