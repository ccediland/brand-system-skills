// Barrel: every PascalCase export here becomes a component the converter discovers from the .d.ts tree.
// BUILDER: add one re-export per real brand component projected from the canon. Invent nothing — each
// component is a projection of GRAMMAR + PRIMITIVES and styles itself only via var(--token-*).
export { Mark } from "./components/Mark.js";
export { Button } from "./components/Button.js";
