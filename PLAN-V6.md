---
name: plan-v6
description: Plan de ejecución DEFINITIVO para construir brand-system-skills v6 sobre la fundación v5 (v0.5.0 @ 9294c961). Se lee al abrir toda sesión v6, se reescribe al cierre de cada sesión, carga Session log propio. Deriva del análisis (brand-system-skills-v5-analysis_2026-07-04.md), de la failure gallery v5 (stress-test-v5/failure-gallery-v5.md, EV1-F01…F15) y de la ronda pre-plan §16b post-gate (F0, 2026-07-16), referenciados por ID. Use al ejecutar cualquier item v6, al decidir secuencia, o al cerrar una etapa.
last_updated: 2026-07-17
applies_to: ccediland/brand-system-skills — baseline v0.5.0 @ 9294c961 → target v0.6.0
status: "DEFINITIVO — F0 cerrada 2026-07-16 (ronda §16b absorbida; preliminar 2026-07-04 superado)"
etapa_vigente: F6
next_action: "F6-01 — integración + docs del repo (RESIDENT/CLAUDE/README) + version bump → v0.6.0"
---

# PLAN v6 — brand-system-skills

> Criterio de la versión (análisis §16e): v6 CONSTRUYE lo no-testeado sobre la fundación que v5 probó, MÁS lo que arrojó el stress-test v5. Fuentes de requisitos y evidencia, por ID: el análisis (congelado) · `stress-test-v5/failure-gallery-v5.md` (EV1-F01…F15 + patrones rectores) · veredicto por rector y R-06 (RESIDENT § Log del ciclo, 2026-07-16). Workflow idéntico a v5 (análisis §16): chat home base, Code brazo de ejecución/análisis, retornos destilados, análisis en cualquier punto puede modificar este plan.

## Reglas de operación

- Las mismas de PLAN-V5.md §Reglas (referencia por ID · 1 item ≈ 1 sesión · fixtures dentro del gate · frescura · análisis permanente §16c · wins-regression por etapa · E-O1: cero push de repos de marca y cero forense al repo, por siempre).
- Branches `claude/v6-f<N>`. Régimen A1 (herencia de la enmienda 2026-07-05): push/PR/merge AUTÓNOMOS al cierre de cada etapa con gates en verde; OK explícito del operador solo donde el home base lo instruya (F0: instruido y cumplido · gate final F6: instruido).
- Regla de contrato único v6: TODO cambio al contrato del wire (verbatim-check, provenance por línea, vocabulario de literales) ocurre UNA vez en F2-01; el resto del plan consume ese contrato congelado.
- **Freeze-after-repin (lección F4):** TODO item que CONGELE un artefacto contra un contrato externo version-fluid (p. ej. el kit contra el pin `/design-sync`) va DESPUÉS de su re-pin vivo, jamás antes. El plan de F4 ordenó emitter→re-pin y el propio `design-sync-kit.md` § Step 0 exige lo inverso ("re-pin the live contract before freezing the emitter"); F0-01c ya sabía que el pin v2.1.185 estaba stale (no cargaba `report_validate`/`.render-check.json`). El re-pin lee PRIMERO el schema del tool runtime (autoritativo) y cae al skill-source + `/update` solo para nombres que el tool no expone; una variante no confirmada en vivo jamás se inventa (cae al genérico confirmado).
- **Verify adversarial PRE-merge (lección F3):** TODO item que introduzca o modifique MECANISMO (regla de gate, terminal de provenance, tool ejecutable) o CONTRATO lleva un verify adversarial ANTES del merge, no después. F3 mergeó con un defecto BLOCKER (sello de ratificación sin content-bind, cerrado en el fix-pack post-F3) precisamente por correr el verify después del merge. El verify PRE-merge es gate de merge; un hallazgo `blocks_merge` bloquea hasta resolverse. Journal del verify respaldado a `/mnt/c/` (forense local-only, jamás al repo — E-O1); si el proceso truena se cosecha del journal, jamás se re-lanza a ciegas.
- Pins: baseline v6 = `9294c961` (commit de disposición de v5, superficie de código). Golden set = `essential-brand` LOCAL @ `8b78dba` (re-baseline E3, F0-04). Golden + failure gallery (v4 local + v5 en repo) = regresión permanente.

## Decisiones tomadas

2026-07-04 (preliminar, vigentes):
- Resolver Module (DTCG): NO en v6 — reemplazaría scheme-derive+R7, mecanismo probado, sin falla ni consumidor que lo exija (P-J-01). Condición de reapertura: un consumidor real que lo pida.
- OI-J build-grade (sonic/motion): FUERA de v6 — post-v6, gated en demanda real de una marca no-visual-primaria.
- F4 emitter = capability del KIT, no entregable propio.
- Mirror Drive = GitHub Action al push (git = fuente de verdad); NO Apps Script. **Creds → GitHub Secrets
  nativo** (enmienda 2026-07-17, DECIDIDO home base): la línea original "creds vía Infisical" nació del mismo
  encuadre equivocado que trató el mirror como infra propia — native-first: el Action YA corre en GitHub, sus
  secrets son la capacidad nativa; exigir un gestor externo a un usuario arbitrario del skill público es una
  capa impuesta. Infisical = nota opcional en el doc, jamás requisito.

2026-07-16 (ronda §16b / F0):
- Nueva etapa F2 "Costura de compilación" + renumeración (Modos→F3 · Kit→F4 · Mirror→F5 · Cierre→F6). Razón: el mecanismo dominante del gate v5 es inflación en COMPILACIÓN (patrón rector 1) — T2 no monta un wire roto; F1 se queda primero (formato antes de regen de fixtures).
- F1-02 = transform del LADO CANON: la restricción del consumer está anclada a bugs de SD v5 (#1398/#1494), no a diseño propio; "consumers are projections" es principio del propio spine; cero toque al spine = golden/gallery intactos.
- EV1-F05 adjudicado: plantillas-redes ⇒ kit YES. El slot se elicita por DESTINO en términos cliente; la spec carga la tabla destino⇒slot (F2-03); el dossier v6 declara el valor esperado (F6-02).
- F4∥F5 arranca tras F2 (no tras F1): F2-01 regenera fixtures de handoff que el arnés e2e del kit consume.
- Sin cambio (adjudicado en el gate): H1 NO-defecto (consolidación con Accountable disuelve — legítimo; residuo = diseño de dossier, F6-02) · NS-E/NS-H sostuvieron · re-scope de EV1-F02 se respeta (BUILD-MODE/kit exentos del frame; su obligación cristaliza en 7b).
- Pendientes del operador (se deciden al llegar a su etapa): perfil de la marca CREATE del stress-test (sugerencia vigente: venture real propio sin material de marca) · cuenta y estructura de folders Drive (F5-01 — RESUELTO abajo).

2026-07-17 (F5-01 — encuadre corregido: el repo es PÚBLICO, el Action se EMITE parametrizado al repo de marca, cada usuario cablea su propio Drive; cuenta+folder = parámetros, jamás stop de build):
- **Creds → GitHub Secrets nativo** (ver enmienda arriba). Infisical = nota opcional.
- **Ruta de auth default: service account + DRIVE COMPARTIDO.** Restricción VERIFICADA
  (developers.google.com/workspace/drive/api/guides/handle-errors): una service account NO tiene cuota y NO
  puede ser dueña de archivos → sube a shared drives o se usa OAuth en nombre de un humano. El patrón "comparto
  una carpeta de Mi unidad con la SA" está ROTO de fábrica (`storageQuotaExceeded`) — el doc lo NOMBRA con su
  evidencia porque es la trampa obvia. Ruta B: OAuth refresh token (para quien no tenga Workspace — los shared
  drives solo existen en Workspace). Scope: Route A (SA) usa `drive` — un folder pre-creado está fuera del
  alcance de `drive.file` (404ea); una SA dedicada queda acotada en la práctica a sus shared drives. Route B
  (OAuth) usa `drive.file` + un root folder app-owned (`GDRIVE_ROOT_FOLDER_NAME`), publicado a producción para
  evitar la expiración de 7 días del token en estado Testing. (Ambos ajustes = cierre de 2 blockers del verify.)
- **Estructura de folders = espejo del ASSET-INDEX, no del árbol del repo** (el asset-index ya es el registro
  canónico de qué es asset y dónde vive, E2-03; la columna Drive lo cierra: por-fila `Drive` o, si `—`, el
  `Kind`). Duplicar el árbol del repo sería estructura sin dueño. Docs-Editors nativos (sin `sha256Checksum`)
  EXCLUIDOS por nombre (R-14).
- **Criterio de gate para capabilities EMITIDAS (doctrina reusable, no solo F5):** el gate de una capability que
  el skill EMITE a un repo de tercero es **emisión correcta de la plantilla + verify OFFLINE que muerde** —
  JAMÁS una ejecución viva que dependa de infra de un tercero. Un round-trip del autor no prueba el skill (prueba
  el Drive/cuenta del autor) y E-O1 prohíbe cablear un repo de marca. La ejecución viva se documenta como PASO
  DE ACEPTACIÓN DEL USUARIO (opt-in) en el doc de operación, no como gate del ciclo. Misma clase que W-3/W-7
  (corridas chat-side que el ciclo no ejecuta). Aplica a F5-mirror y a toda capability emitida futura.

## Dependencias

v6 ← v5: F2-01 ← E1-02 (contrato v5 congelado; bump único v6) + E3-01 (máquina/ledger/curator-wall) · T2 ← E1-02 + E3-01 · CREATE ← E1-04 (rama sin-fuente, ya instruida en run-gates) · F4 ← E1-09 (arnés e2e) + E0-01 (workaround Onyx @ `025cc64`, verificado F0-01d) · mirror ← E2-03 (asset-index) + E1-05 (custodia) · migración ← golden congelado ✓.
Internas v6: F1 → F2 (formato primero) · F2 → F3 (T2 monta el wire reparado) · F2 → F4∥F5 · todo → F6.

## F0 — Re-verificación (CERRADA 2026-07-16)

| Item | Resultado | Estado |
|---|---|---|
| F0-01 | Proyección FASE B verificada contra el repo real: a) migración vigente, line-refs frescos (ver F1-01) · b) CREATE re-scoped: completar downstream, no construir de cero (seams v5 sustanciales existen) · c) /design-sync VIGENTE (pin v2.1.185 sin report_validate/.render-check.json) · d) prerequisito Onyx cumplido (`025cc64`) · e) recalibración absorbida en Dimensionamiento | done |
| F0-02 | Hop downstream ROTO desde C-1 (no solo línea stale): web-stack exige `$value` string (tokens-example.md:7/:15/:81/:84 · SKILL.md:94; SD v5 #1398/#1494); el spine emite objeto estructurado (base.json:7). Clon en `~/proyectos/web-stack-skills`. Decisión → F1-02 | done |
| F0-04 | Re-baseline E3 del golden (EV1-F11 H1-r3): `expected-refusal-contract.md` extraído de battery.md como archivo propio (exigido por run-gates.mjs:299-302), commit LOCAL `8b78dba` cero push; perfil run-3 sin clase interina (su reaparición = hallazgo) + pin actualizado; gates re-corridos = solo clases conocidas, cero clase nueva, frozen restaurado porcelain 0 | done |
| F0-03 | Este documento: preliminar → DEFINITIVO con F0-01/F0-02/F0-04 + insumos del gate trazados | done |

Gate F0: CUMPLIDO — proyección validada/corregida + veredicto del hop + plan definitivo. PR del gate con OK del operador (branch `claude/v6-f0`).

## F1 — Spine (formato primero: F2+ regenera fixtures — migrar después obligaría doble regen)

| Item | Contenido → entregable | Esf. |
|---|---|---|
| F1-01 | Migración `.tokens.json` (cierra deferral E1-08 + CONTRA-9). Código con cambio real: SOLO `scheme-derive.mjs` (:8 comment · :84-85 reads base/semantic · :157 write schemes); `audit-lint.mjs:124/:128` y `package-validate.mjs:86` sufijo-compatibles vía endsWith — sin cambio. Renombres: 21 archivos token (3 templates + 18 fixtures, incl. `schemes/*.json`). Prosa: ~25 líneas en ~15 archivos (token-spine :11-13 deferral a RETIRAR + :32-34,:45 · validate-audit :101,:171,:212 · SKILL :36,:268-271 · visual-keystone :25,:27 · prototype.html ×4 · styles.css ×2 · docs/CLAUDE :23 · docs/RESIDENT :55 · 03-grammar :61 · projections :33 · descripciones de fixtures ×~8). CHECKSUMS sin regen (cero refs a tokens/); surfaces/asset-index migration-proof. Golden/gallery pasan sin rename (verificado F0-01a) | S–M |
| F1-02 | Transform del lado canon (veredicto F0-02): proyección string OKLCH derivada del `$value` estructurado, emitida como salida propia del tooling del spine — el spine NO se toca; e2e contra el clon `~/proyectos/web-stack-skills`; des-stalear RESIDENT §Integrations (flag 2026-07-04 resuelto). Secuencia: post-F1-01, sobre el layout renombrado | S–M |

Gate F1: suite completa verde con la extensión nueva + golden re-verificado + hop downstream end-to-end en verde.

## F2 — Costura de compilación (NUEVA — scoper wire-integrity)

> Responde a los dos patrones rectores del gate v5: (1) inflación en el paso de COMPILACIÓN — specifics sin etiqueta entran a carriers RATIFIED{} después del último checkpoint del owner, donde ningún gate corre; (2) compresión de instrumentos bajo presión. T2 (F3-02) consume este wire reparado.

| Item | Contenido → entregable | Esf. |
|---|---|---|
| F2-01 (×2) | Contrato wire v6 — cambio único lockstep: **verbatim-check compilado-vs-brief-firmado** (todo literal del wire marcado ratificado/verbatim/"sus palabras"/owner-confirmed se verifica contra el texto firmado — machine-checkable, gate del compile) · **PROVENANCE por línea** dentro de `RATIFIED{}` y en filas `not-used(owner-declared)` — el wrapper deja de heredar ratificación a paráfrasis derivada (EV1-F13, mecanismo de F03/F04); blanket jamás acuña not-used (EV1-F10) · vocabulario saneado: 6 clases de literal fuera de contrato sancionadas-o-corregidas (EV1-F12) + "n/a" fuera, born-gap shipea como fila GAPS (EV1-F07) · CREATE jamás false-bloqueado: sin brief firmado = N/A declarado (W-15) · propagación lockstep a las superficies del ladder + regen de fixtures de handoff | L |
| F2-02 | Gate-3.5 emite-o-waive-con-ledger: la 3.5 emite su deliverable o el waive muestra el LEDGER de costo con las dimensiones UNOPENED nombradas (EV1-F01) · barrido pre-brief: el compile 7a no procede sin ledger de dimensiones completo — cada dimensión en estado terminal o listada UNOPENED en "Qué falta" del brief (EV1-F02) · la instrucción de retome carga el brief (residuo EV1-F06) | M |
| F2-03 | Reglas escritas + checks chicos: corrección post-firma ⇒ REEMISIÓN fechada obligatoria, manifest apunta a la versión corregida, hash debe cambiar (EV1-F08) · no-✔ antes del pase To-confirm (EV1-F14: 8 promotes inventariados) · prosa cliente jamás referencia cuenta/operador/plumbing (EV1-F09 / NS-F) · tabla destino⇒kit-slot + elicitación del slot en términos cliente (EV1-F05) | S–M |

Gate F2: fixtures nuevos MUERDEN (wire con paráfrasis sin provenance → FAIL · skip de la 3.5 sin ledger → FAIL · not-used por blanket → FAIL) + suite completa verde + W-18/W-19 + wins-regression íntegra.

## F3 — Modos (CREATE primero, T2 después)

| Item | Contenido → entregable | Esf. |
|---|---|---|
| F3-01 | CREATE completado (re-scope F0-01b: completar downstream, no construir de cero). Existente v5: handoff-format :65/:126/:208 (MODE parse-or-stop) · run-gates :157-159/:195/:210 (NOT-RUN + instrucción master-autorado, jamás false-block) · validate-audit :267-270 · SKILL ruteo + seam Stage 8. Falta: create.md gana seams per-stage (fidelity/audit/kit/keystone — hoy 0 menciones ahí) y EJECUTA la rama sin-fuente que run-gates instruye · disciplina de provenance CREATE · reproduction-router: seam o N/A explícito (asume fuente capturada) · keystone-emit: adjudicar mode-agnóstico (probable por construcción — lee confianza DEL token) · doctrina intacta (jamás re-elicitar el WHY; GAP del handoff queda GAP) | M |
| F3-02 | T2 EXTEND/RECOMMEND: modo híbrido ANALYZE + propuestas etiquetadas — carrier per-dimension del contrato v5 EXTENDIDO por F2-01 (provenance por línea); consume el contrato v6 congelado, cero bump propio · flujo builder draft-desde-recomendación · loop de ratificación post-primer-feedback · hereda el curator-wall ESCRITO y el 4º estado (v5 E3-01) | M–L |

Gate F3: fixtures sintéticos de modo (handoff CREATE todo-vacío · handoff T2 con dimensiones proposed) pasan la suite completa; la validación viva queda en F6.

## F4 — Kit (paralelizable con F5 tras F2 — superficies disjuntas entre sí; ambas consumen fixtures post-F2)

| Item | Contenido → entregable | Esf. |
|---|---|---|
| F4-01 (×2) | F4 emitter offline de static cards como capability del kit: tool HTML estático sin React/network sobre el arnés e2e de v5 (E1-09); prototipo de referencia = workaround de Onyx congelado (`onyx-brand-canon` @ `025cc64`, `claude-design-upload/`: 4 cards + preview — prerequisito VERIFICADO F0-01d); fixtures propios; las static cards entran al manifest/deny como superficie cliente | L |
| F4-02 | Re-pin del contrato /design-sync: absorbe `report_validate` / `.render-check.json` — el pin v2.1.185 NO los carga; único registro en fixtures/kit-e2e/README-FIXTURE.md:28 (F0-01c). Regla de frescura al implementar | S |

Gate F4: fixture e2e extendido cubre el emitter (build → cards estáticas → package-validate → deny) en verde.

## F5 — Mirror Drive

| Item | Contenido → entregable | Esf. |
|---|---|---|
| F5-01 | GitHub Action al push: upload de binarios SIN conversión + verify `sha256Checksum` contra CHECKSUMS · columna Drive del asset-index (base additiva de v5 E2-03) · doc de operación · **creds vía GitHub Secrets** (enmienda 2026-07-17; Infisical opcional). Estructura de folders = espejo del asset-index; auth SA+shared-drive / OAuth. Docs-Editors excluidos explícitos (sin checksum — R-14). **DONE (código + verify CLEAR); gate F5 = round-trip vivo del operador** | M |

Gate F5: round-trip verificado — push → Drive → checksums match en ambos lados.

## F6 — Cierre v6

| Item | Contenido → entregable | Esf. |
|---|---|---|
| F6-01 | Integración + docs del repo (RESIDENT/CLAUDE/README) + version bump → v0.6.0 | M |
| F6-02 | Diseño fino del stress-test v6 (reglas §11 + lecciones del gate v5): corrida CREATE de cero (perfil = decisión del operador) · corrida T2 (marca existente + extensión propuesta) · AMBOS brazos del kit cubiertos: kit-YES con wire emitido + v0/DEMO (delta-2 v5 quedó roto; NS-C sin cobertura) · dry-parse leg builder-side OBLIGATORIO por corrida (NS-B no-ejercitado en v5) · ambas corridas EMITEN handoff — controla la asimetría de superficie que invalidó R-06 y re-corre el probe · el dossier declara el expected-slot (EV1-F05) y usa cebos que NO dependan de que el owner no decida (H1-residual) · runbook: transcript-capture pre-cierre cero reconstrucciones, aborto ⇒ registro, delta Incógnito-no-ciega-cuenta declarado, handoff.md a raíz + instrumentos descargables reales + pin fresco (EV1-F15) · regresión de la suite v5 COMPLETA (golden `8b78dba` + gallery) | M |
| F6-03+ | Corridas + disposición: veredicto por capability (CREATE · T2 · F4-emitter · mirror · migración · COSTURA — sostuvo o no, con evidencia) + failure gallery v6 | L |

Gate final — disposición de v6; los hallazgos alimentan el ciclo siguiente.

## Wins-regression

La tabla W-1…W-14 de PLAN-V5.md corre ÍNTEGRA en el gate de cada etapa v6, más 5 checks nuevos:

| W+ | Check |
|---|---|
| W-15 | CREATE jamás false-bloqueado por gates sin-fuente (fixture todo-vacío en verde; W-1 extendido a modo) |
| W-16 | Propuestas T2 jamás canonizadas sin ratificación (toda dimensión proposed conserva hypothesis+GAP hasta sign-off) |
| W-17 | Integridad del mirror: **NOT-RUN honesto** (harness offline listo). El harness `--plan`/`--verify` MUERDE (custody stale · colisión · binary-mode · empty-pass · mismatch · exclusión Docs-Editors); el round-trip VIVO (checksums match ambos lados) es **aceptación del usuario, no gate del ciclo** — el skill emite y verifica la PLANTILLA, no el Drive de un tercero, y E-O1 prohíbe cablear un repo de marca del autor (clase W-3/W-7) |
| W-18 | Cero specific sin provenance por línea dentro de `RATIFIED{}` / `not-used(owner-declared)` — fixture con paráfrasis derivada falla sin etiqueta |
| W-19 | La gate-3.5 emite o el waive muestra el ledger con las UNOPENED nombradas — fixture de skip falla |

## Dimensionamiento (inferring — se recalibra por etapa)

F0 consumida (1 sesión real vs 2–3 proyectadas) · F1 ≈ 2 · F2 ≈ 3–4 · F3 ≈ 3–4 · F4 ≈ 3 · F5 ≈ 1–2 · F6 ≈ 3–4 → restante ≈ 15–19, total ≈ 16–20 (vs 14–19 del preliminar sin F2).

## Session log

- 2026-07-17 — **GATE F5 CERRADO — ETAPA F5 CERRADA (criterio re-definido; branch claude/v6-f5; push+PR+merge A1).** **DECIDIDO del home base:** el round-trip VIVO del mirror **NO es gate del ciclo** — el mirror es capability OPT-IN que cada usuario cablea con su propio Drive; un round-trip del autor prueba el Drive del autor, no el skill, y E-O1 prohíbe cablear un repo de marca. **Gate F5 re-definido:** criterio = la plantilla EMITE correcta + el verify OFFLINE muerde (ya cumplido en F5-01: `--plan`/`--verify` deterministas + fixtures + 4 bloqueantes cazados pre-merge + re-verify CLEAR). **W-17 → NOT-RUN honesto** (clase W-3/W-7): el harness offline muerde, el round-trip vivo es aceptación del usuario. **Doctrina reusable asentada** (§Decisiones): el gate de TODA capability emitida = emisión correcta + verify offline, jamás ejecución viva dependiente de infra de tercero. **Ajustes de doc (código intacto):** W-17 con nota · checklist de cableado re-titulado "Drive-mirror — user acceptance (optional)" · `DRIVE-MIRROR.md` gana encuadre OPT-IN explícito. **GATE F5 (output real):** suite **30/30** · golden `essential-brand @ 8b78dba`: audit 0 + 8 clases FAIL exactas, §3d "drive mirror plan" N/A (cero clase nueva), porcelain 0 · wins W-1…W-19: muerden W-15/16/18/19 + el harness offline del mirror (custody-stale · colisión · binary-mode · empty-pass · mismatch · exclusión); **NOT-RUN honesto: W-17 (round-trip vivo = aceptación usuario) · W-3/W-7 (corridas chat-side → F6)** · scrubs 0/0 · caps scoper 500/500, builder 464/500. Etapa vigente → F6.
- 2026-07-17 — **F5-01 done (código + verify CLEAR) — branch claude/v6-f5, SIN PR (el gate F5 exige el round-trip EN VIVO con creds reales; la etapa NO cierra aquí).** Mirror Drive = GitHub Action EMITIDO al repo de marca (encuadre corregido: repo PÚBLICO, Action = plantilla parametrizada, cada usuario cablea su Drive). **DECIDIDOs del home base** (§Decisiones 2026-07-17): creds→GitHub Secrets nativo (Infisical opcional) · auth default SA+shared drive con la restricción `storageQuotaExceeded` NOMBRADA con evidencia + ruta B OAuth · folders = espejo del asset-index. **Entregables:** `tools/drive-mirror.mjs` (zero-dep, 3 modos: `--plan`/`--verify` OFFLINE deterministas + live) · `.github/workflows/drive-mirror.yml` (push a rama default SOLO, jamás `pull_request_target`; `permissions:contents:read`; secrets solo `env:`) · `.github/DRIVE-MIRROR.md` (doc op) · `references/drive-mirror.md` (método) · run-gates **§3d "drive mirror plan"** (corre `--plan` OFFLINE si el workflow está emitido; N/A si no) · columna Drive del asset-index nombrada como mapa del mirror · surface `.github/**|operator` · checklist de cableado a raíz. **Contrato del mirror:** set = asset-index ∩ CHECKSUMS · no-conversión (MIME real, Docs-Editors excluidos por nombre R-14) · verify sha256 AMBOS lados sin empty-pass ni stale · one-way (P-J-01). **VERIFY ADVERSARIAL PRE-MERGE (6 atacantes + juez, journal a /mnt/c ANTES):** los 4 frentes de SEGURIDAD (conversión · empty-verify · stale-binary · **secret-exfil/pull_request_target**) LIMPIOS; los 2 de corrección cazaron **4 BLOQUEANTES, todos corregidos + re-verify CLEAR** (`wf_8ac6a2b4-ffa` + re-verify `wf_731f444b-23e`): B1 `drive.file` no escribe a folder pre-creado→live 404 (fix: Route A scope `drive`; Route B app-owned root `GDRIVE_ROOT_FOLDER_NAME`) · B2 OAuth refresh token 7-días en Testing (fix: doc publica a producción, drive.file no-sensible) · B3 parser CHECKSUMS tira binary-mode `hash *path`→mirror vacío silencioso (fix: `\*?` en regex; fixture `checksums-binary-stale`) · B4 `//` interno escapa colisión→pérdida silenciosa (fix: `canonFolder` calca el resolver vivo; fixture `collision-double-slash`). **GATE del item:** suite **30/30** (incluye 8 fixtures drive-mirror + 2 regression guards nuevos) · golden `essential-brand @ 8b78dba`: audit 0 + 8 clases FAIL exactas, §3d N/A (cero clase nueva), porcelain 0 · scrubs 0/0 (cero cycle-ID/marca/org en emitidos) · caps SKILL (scoper 500/500, builder 464/500). Design note `design-note-f5-01-drive-mirror—2026-07-17.md`. La regla §Reglas :21 (verify PRE-merge) volvió a pagarse sola: F5 habría mergeado dead-on-arrival + con empty-pass silencioso en gate BLOCKING. Etapa F5 sigue ABIERTA (gate = round-trip vivo del operador); RESIDENT intacto.
- 2026-07-17 — **F4-02 done + GATE F4 CORRIDO — ETAPA F4 CERRADA (branch claude/v6-f4; push+PR+merge autónomos A1).** **HALLAZGO del home base:** F4-01 congeló el emitter contra el pin v2.1.185 SIN re-pinear — violando el Step 0 del propio `design-sync-kit.md` ("re-pin the live contract before freezing the emitter") y F0-01c (que ya sabía que el pin no cargaba `report_validate`/`.render-check.json`). El orden del plan estaba invertido; F4-02 lo endereza (nada shippeó — la etapa no mergeó). **Re-pin VIVO (nuevo método, mejor que /update):** el **schema del tool `DesignSync`** ES la interfaz runtime autoritativa — lectura viva directa, sin `/update` ni skill-source. **Tabla de divergencia (pin → live → veredicto):** `@dsCard` first-line marker → LIVE-CONFIRMADO current (el emitter es correcto, NO-OP) · card index "ds_manifest" → **`_ds_manifest.json`** (refinado) · `register_assets`/`unregister_assets` → LEGACY (el marker los reemplazó) · **`report_validate` → método LIVE** + **`.render-check.json`** → artefacto LIVE con `counts {total,bad,thin,variantsIdentical,iterations}` (los DOS que el pin v2.1.185 MISSED y el read del 2026-07-06 vio — AÑADIDOS al pin) · flujo upload `create_project→finalize_plan→write_files/delete_files` (get_file 256 KiB, write_files 256/call) confirmado · `group` = free-form (labels live: Type/Colors/Spacing/Components/Brand — el emitter usa "Color" singular, variante válida) · config.json keys + scripts converter NO tool-visibles → quedan en el pin F0-01c-confirmado (variante no confirmada jamás se inventa). **Reconciliación:** `design-sync-kit.md` Step 0 (método de lectura viva) + § nueva de `report_validate`/`.render-check.json` + `_ds_manifest.json` + legacy-register + note NO-OP-on-emitter; templates `preview-card.html`/`README-KIT.md` → `_ds_manifest.json`; arnés kit-e2e extendido con la pierna offline del emitter (build → cards → --check → deny). **EMITTER = NO-OP en código** (usa solo el `@dsCard` marker live-confirmado; 0 refs stale) → **el verify adversarial de F4-01 se SOSTIENE, NO corrí uno nuevo** (Step 2 del re-scope: no-op en código ⇒ solo prosa/pin). **GATE F4 (output real):** suite **36/36** verde (emit-cards clean/violation · run-gates §3c static cards · deny) · golden `essential-brand @ 8b78dba`: audit 0 + run-gates 8 clases FAIL exactas, cero clase nueva, porcelain 0 (el row §3c es N/A en el golden) · pierna offline del emitter corrida verde (emit→--check→deny; package-validate = [NO_DIST] honesto sin npm) · wins W-1…W-19 íntegra (NOT-RUN honesto solo W-3/7→F6 y W-17→F5) · scrubs 0/0 (cycle-IDs F4-0x limpiados de tool/referencia/README) · caps SKILL intactos (0 SKILL tocado). **Regla nueva §Reglas :20 — freeze-after-repin:** todo item que congele un artefacto contra un contrato externo version-fluid va DESPUÉS de su re-pin. Etapa vigente → F5 (mirror Drive).
- 2026-07-17 — **F4-01 done (branch claude/v6-f4; SIN PR — la etapa F4 vive hasta F4-02; design note `design-note-f4-01-offline-cards-emitter—2026-07-17.md`).** El **emitter offline de static cards** (`tools/emit-cards.mjs`, zero-dep, capability del kit), derivado del prototipo Onyx congelado (@ `025cc64`, READ-ONLY E-O1) y sus reports (`DESIGN-SYNC-SKILL-REPORT.md` F4/P4: el kit no produce nada renderable offline — sus cards React necesitan build networked + `_ds_bundle.js` + el converter; el workaround fue autorar 4 static cards a mano). **Qué hace:** lee el canon (`tokens/*.tokens.json` · `tokens/schemes/*` · `canon/canon.json` · `canon/mark.svg`) y escribe cards `@dsCard` self-contained a `design-sync-kit/cards/NN-<group>.html` — una por capa PRESENTE (Brand si hay mark · Color · Type · Components), NO React/bundle/converter/**network**. Hace de `[NO_DIST]` un estado de handoff revisable. **Mejora sobre el workaround:** verdaderamente offline — CERO refs remotas (font STACKS con fallbacks del sistema, jamás `@font-face` CDN; Onyx aún cargaba fuentes de jsdelivr). **Honestidad de provenance (materializar confiere NADA):** un valor incierto (test R5) renderiza "· provisional" en vocab cliente, jamás GAP id/grade — incluye schemes (scheme-derive materializa todo scheme a hypothesis+GAP → un scheme materializado NO está settled). **Superficie cliente:** las cards caen bajo la fila `design-sync-kit/** | client` existente → el deny las scrubbea (cero plumbing nuevo). **Custody de derivados (patrón F1-02):** una entrada `sources/MANIFEST.json` por (card × input canónico); el color-file es parent de TODA card (todas embeben el color closure) → cambio en el canon = STALE FAIL; una capa REMOVIDA se poda (el emitter borra sus cards huérfanas al re-emitir); la mark lleva `id="brand-mark"` → R6b la reconcilia. **Gate:** `--check` (offline: [REMOTE_REF]/[DSCARD_MISSING]) = fila **static cards** §3c de run-gates (N/A sin cards/kit). Fixtures `gates/emit-cards/{clean,violation}`. **VERIFY ADVERSARIAL PRE-COMMIT (4 frentes + juez, journal a /mnt/c):** cazó **7 defectos, 5 bloqueantes, todos corregidos** — BLOCKER scheme-materializado-sin-marcar · MAJOR mark `<image href="http:host">` (esquema sin `//`) evadía el REMOTE regex · MAJOR **inyección** (`$value` sin escapar en `<style>` → breakout `</style><img src=http:…>`) · MAJOR card huérfana al quitar capa · +2 no-bloqueantes (chrome inks + custody del color closure) también arreglados. Fixes: marker de scheme lee confidence de roles · REMOTE regex flag a cualquier esquema no-`data:` en posición fetchable · sanitizers `safeColor`/`safeFont` (valor mal formado = dropeado) · pruning de huérfanas · color-file parent universal. **Cierre:** suite **36/36** · golden 8 clases exactas, cero nueva, porcelain 0 · cards verificadas visualmente (4 renders) · scrubs 0/0 · caps SKILL intactos (0 SKILL tocado; wire-in a Stage 8 vía `references/design-sync-kit.md`). F4-02 NO arrancado; RESIDENT intacto (gate de etapa).
- 2026-07-17 — **OI-K CERRADO — path-bind + section-scope del content-bind de ratificación (branch claude/v6-oik; push+PR+merge autónomos A1; verify note `verify-note-oik-path-section-bind—2026-07-17.md`).** Aprieta el helper existente (cero motor nuevo, cero delta de contrato). **Dos ataduras en R3:** PATH-BIND (valor en una línea que nombra el slot del token — X para el slot A ya no ratifica al slot B con X) · SECTION-SCOPE (solo `## What was ratified` ratifica; bloque rechazado/superseded inerte — inversión del run 2 v5). Golden-safety verificada antes de apretar (0 registros de ratificación en `8b78dba`). **Verify adversarial PRE-merge (3 frentes + juez, journal a /mnt/c):** 3 bloqueantes cazados y corregidos — value-residue en la misma línea (fix: VALUE SPAN, primer span backtick tras el slot) · heading superseded/nested (fix: exacto/top-level/único) · falso-FAIL por sub-headers ATX (resuelto seguro + documentado bold-grouping; la opción B se rechazó por reabrir el reject-sub-header). Fixtures nuevos `ratified-line-residue` + `ratified-superseded-heading` (+ los base `ratified-cross-slot`/`ratified-rejected-block`). **Cierre:** suite **32/32** · golden 8 clases exactas, cero nueva, porcelain 0 · scrubs 0/0 · caps SKILL intactos. Convención en gap-protocol § record shape. OI-K → CERRADO (residual: solo la frontera self-serving-record, declarada-open sin cambio). La regla §Reglas :20 (verify PRE-merge) volvió a pagarse sola. Etapa vigente sigue F4.
- 2026-07-17 — **FIX-PACK POST-F3 — content-bind sobre el acto de ratificación (branch claude/v6-f3-fix; push+PR+merge autónomos A1; verify note `verify-note-f3-fix-ratification-content-bind—2026-07-17.md`).** F3 mergeó con un defecto BLOCKER porque su verify corrió DESPUÉS del merge — la lección que agrega la regla nueva a § Reglas de operación (verify adversarial PRE-merge para todo item con mecanismo/contrato). Un verify post-F3 (huérfano por doble crash de WSL: 3/5 atacantes + A rescatado + C parcial, Judge nunca corrió — cosecha forense local-only) cazó el hueco; el home base adjudicó A-F3-01 (BLOCKER) + A-F3-02 (MAJOR) como UN defecto. **El hueco:** un `owner-confirmed` citando `sources/ratification—<fecha>.md` genuino+hasheado subía a la cima SIN que nada verificara que el registro NOMBRA el valor (R3 ataba hash→path; `valueInText` de R1 solo en `corroborated`; la forma RATIFY legítima daba exit 1 por R2, forzando el relabel-a-owner-stated = lavado). **El fix (un mecanismo, cero delta de contrato):** (1) content-bind del acto de ratificación en R3 — el registro debe EXISTIR y nombrar el valor CANÓNICO (análogo post-handoff del `BRIEF{}` verbatim); (2) terminal `ratified-proposal` (source enum nuevo — DECIDIDO: valor nuevo, NO carve R2/R5; preserva el invariante `proposed`=cuarentena y graba el linaje en el nombre, jamás relabel a owner-stated); (3) `gap-protocol § loop de ratificación` reescrito ejecutable + nombra el eje source (corrido contra el linter). **E1/E2 (P-J-01):** E1 ghost-record MUERTO para ratificación (existsSync); E1-ghost-handoff + E2 owner-confirmed-sobre-handoff SOBREVIVEN declarados-open (cerrarlos rompe el golden). **VERIFY PRE-MERGE (3 frentes + juez, journal a /mnt/c):** cazó 6 huecos new-in-fix en la 1ª implementación (reusaba el `valueInText` LAXO de R1 para un sello que necesita matching ESTRICTO) — **4 bloqueantes CORREGIDOS** (BLOCKER hex short-circuit: OKLCH fabricado shippeaba si el hex estaba en el registro · 3 MAJOR: substring de fuente "Ares"⊂"shares" · mismo short-circuit en primario · content-bind vacío para no-color) vía helper estricto content-bind-local `ratificationNamesValue` (exige componentes OKLCH nunca hex-solo · matches word/number-bounded · forma no-verificable = FAIL; `valueInText` intacto = R1 sin regresión) + **2 DOWNGRADED a residual declarado** (OI-K: value-bound no slot-bound). Fixtures gemelos `fixtures/gates/t2-proposed/`: `ratified-clean` (PASS, antes no-linteable) · `ratified-mismatch` (FAIL content-bind) · `ratified-hex-held` (FAIL, el BLOCKER del verify). **Cierre:** suite **28/28** verde · golden `essential-brand @ 8b78dba`: audit 0 + 8 clases exactas, cero nueva, porcelain 0 · scrub de cycle-IDs (README fixture + comentario tool; scrubs 0/0). Etapa vigente sigue F4 (el fix no la avanza).
- 2026-07-17 — **F3-02 done + GATE F3 CORRIDO — ETAPA F3 CERRADA (branch claude/v6-f3; push+PR+merge autónomos, régimen A1).** T2 EXTEND/RECOMMEND diseñado CONSUMIENDO el contrato congelado — CERO delta de shape (la representación de cuarentena v5/F2-01 se usa tal cual). **Diseño (4 piezas):** (1) **T2 = ENGAGEMENT SHAPE de ANALYZE, jamás tercer literal de MODE** — el wire shipea `MODE: ANALYZE`; cada propuesta invitada viaja el canal de cuarentena etiquetado (línea `PROPOSED` / WHAT slot a `source:proposed · confidence:hypothesis`, montada en su fila GAPS client-language); una propuesta jamás porta `BRIEF{}` (su linaje ES el label `proposed` — wire-check ya la exime, :146/:241, verificado con valor real) ni re-fila su dimensión. Mecanismo: § nueva "The EXTEND/RECOMMEND engagement (T2)" en elicitation-machine.md — mitad analizada corre el lifecycle normal (provenance original conservada); mitad propositiva corre OPEN → PROPOSED-QUARANTINE con **invitation-scope**: el ask del engagement NOMBRA los slots invitados (invitación de registro en el ledger; guard (a) satisfecho ahí DENTRO, doble guard intacto FUERA — jamás licencia blanket); curator wall aplica ÍNTEGRO vía su cláusula de herencia escrita ("any future propose-mode inherits as written" — ya existía, cero edición). (2) **Loop de ratificación post-primer-feedback** (escrito en ambos lados): el PRIMER feedback del owner sobre una propuesta abre el loop con exactamente DOS salidas — RATIFY (acto registrable: promote gate-6 registrado en el brief pre-handoff; post-handoff `sources/ratification—<fecha>.md` hasheado, el shape owner-confirmed existente; gap cierra) o ADJUST-and-stay-proposed (valor revisado, sigue proposed+hypothesis+gap). SIN tercera salida ni salida implícita: silencio, entusiasmo en prosa y USO del draft promueven NADA (operar sobre cuarentena es para lo que existe el canal). (3) **Flujo builder draft-desde-recomendación** — § nueva "Draft-from-recommendation" en gap-protocol.md: el builder PUEDE materializar drafts (tokens/renders/cards) desde una propuesta; TODO lo materializado hereda `source:"proposed"·confidence:"hypothesis"` montado en el MISMO gap (R2/R5 sostienen el cap mecánicamente); materializar confiere NADA; ruteo en builder SKILL:137-140 extendido (462/500). (4) **Doctrina:** T2 EXTIENDE, jamás re-litiga — propuesta que contradice línea ratificada se surfacea como tensión (disconfirming-facts), jamás reopen silencioso; lo analizado conserva provenance original. Scoper SKILL: bloque T2 en § Doctrine (3 líneas) + ruteo en Stage 1 — **500/500 pagado**: doctrine intro −2 (parentético greenfield comprimido) · "skip the scoper" −1 · § 1 Mode determination −1 (fundido con puntero T2). **Fixture NUEVO `fixtures/gates/t2-proposed/` (gemelos, Fixture Delta):** `clean/` = wire T2 v6-válido (2 líneas PROPOSED + WHAT slot proposed + dims extensión `seasonal-accent`/`motion` tagged-gap + brief que nombra ambos drafts como pendientes) → wire-check **PASS 14/12/2** (exención de cuarentena probada con valor real) + token draft materializado (`source:proposed·hypothesis·gap:GAP-001`, RESIDENT ledger OPEN) → **audit-lint exit 0 CLEAN** (el canal bien portado jamás bloquea) + board: audit/wire/kit-opt-out PASS (resto = filas estructurales genéricas). `violation/` = el MISMO wire pero token canonizado (`owner-confirmed`, gap dropped) → **audit-lint exit 1: R2 + R3 + R5** (tres facetas del mismo crimen: subió sin acto · sin record hasheado · gap de cuarentena soltado) = **W-16 MUERDE con output real**. **GATE F3 (output real):** suite completa verde — audit 0/1/0/0 · r1 0 · gates clean 2 / violations 1 · custody pass/fail + kit-off par (filas propias) · deny 0/1 · scheme-derive 0 · tokens-project 0 · wire-check: 8 FAIL + 2 N/A honestos (empty-create.md + create-empty/) + 5 PASS (3 handoffs + clean + t2-clean) · create-empty: N/A + §7a NOT-RUN CREATE + opt-out PASS (**W-15 sigue mordiendo**) · **golden `essential-brand` @ `8b78dba`: audit 0 + run-gates con las 8 clases FAIL exactas del baseline, cero clase nueva, porcelain restaurado 0** · wins W-1…W-19 íntegra: muerden W-1 (flat/sonic 0) · W-2 (9 markers + panel) · W-5 (4 proposed capadas — la 4ª es el draft T2) · W-9 (deny seeded 1) · W-10 (parity 0) · W-11 (violations GAP rows) · W-12 (board) · W-15 (create-empty output) · **W-16 (t2 twins output)** · W-18 (untagged/honest-quote/blanket FAIL) · W-19 (silent-dimension FAIL); texto shipped W-6/W-8/W-13/W-14; NOT-RUN honesto SOLO W-3/W-7 (corridas → F6) · W-17 (mirror → F5) · scrubs 0/0 (único hit de ciclo = label "v6 F2-01" pre-existente del baseline F2 en README de fixture no-emitido) · scoper 500/500 · builder 462/500. Deuda F3-01 saldada: conteo de fixtures wire-check en CLAUDE.md raíz corregido (6→8 FAIL) + fixtures de modo documentados. Etapa vigente → F4.
- 2026-07-17 — **F3-01 done (branch claude/v6-f3)** — CREATE completado DOWNSTREAM (re-scope F0-01b ejecutado: completar, no construir de cero; cero delta al contrato congelado). **Forma de cada seam:** (1) **create.md §5 nuevo — seams per-stage:** treatments (router § Mode seam; selección de método mode-agnóstica; vector-trace inaplicable salvo artwork del owner) · fidelity — la rama sin-fuente ahora se OPERA, no solo se cita: el MASTER AUTORADO es el source-of-record (`fidelity-diff --source <master> --reproduction <emitido>` por cada NON-WAIVABLE con master; drift master↔render es drift real; scores.json persistido); sin master aún = NOT-RUN del runner o medium N/A — jamás FAIL por carecer de lo que CREATE por definición carece (W-15) · audit (wire-check resuelve por contenido del wire: todo-vacío = N/A; CREATE con WHY ratificado = check completo) · kit (sin cambio — slot por destino del handoff, cards desde el canon autorado) · keystone (mode-agnóstico, ver 4). (2) **create.md §6 nuevo — disciplina de provenance CREATE:** todo lo generado = `source:"authored"` · `confidence:"hypothesis"` (ejes ortogonales) montado en gap "pending owner ratification"; valores del owner conservan el estatus de su línea; `verbatim`/`owner-confirmed` sin acto del owner NO EXISTEN (estructural vía v6 + disciplina escrita: un valor autorado que luego gusta se RATIFICA entonces, jamás se re-etiqueta retroactivo). (3) **reproduction-router § Mode seam (adjudicado, no silencio):** selección mode-agnóstica; validación por SUSTITUCIÓN del source-of-record (master autorado donde iría la captura Stage-5); archived-source gate N/A en CREATE. (4) **keystone-emit § Mode note — VEREDICTO: MODE-AGNÓSTICO por construcción, verificado contra el reference real** (lee confianza DEL token/canon/carrier — :86-88 — jamás cómo se obtuvo el dato; en CREATE las mismas ramas hypothesis/GAP simplemente disparan más; cero paso CREATE-específico necesario) — declarado explícito, el silencio ya no es el estado. (5) Doctrina intacta reforzada: WHY jamás re-elicitado, GAP queda GAP, ningún seam re-abre elicitación (cierre de §6). SKILL builder: loader de create.md actualizado (460/500). **Fixture NUEVO `fixtures/gates/create-empty/`** — repo mínimo con el wire CREATE todo-vacío v6-válido corrido por la SUITE: wire-check N/A(declared) · §7a mandatory set NOT-RUN con la instrucción master-autorado (2 carriers) · kit opt-out PASS — **W-15 MUERDE con output real** (filas restantes = estructurales genéricas de fixture mínimo, documentadas en su README). **Gate del item:** suite completa verde (audit 0/1/0/0 · r1 0 · gates 2/1 · custody/kit-off baseline · deny 0+0/1) · wire-check: 8 FAIL + 2 N/A honestos (empty-create + create-empty) + 3 handoffs y clean PASS · golden: audit 0 + 8 clases exactas del baseline `8b78dba`, cero nueva, porcelain 0 · scrubs 0/0 · scoper 500/500 intacto · builder 460/500. F3-02 NO arrancado; RESIDENT intacto (gate de etapa).
- 2026-07-17 — **F2-03 done + GATE F2 CORRIDO — ETAPA F2 CERRADA (branch claude/v6-f2; push+PR+merge autónomos, régimen A1).** **Las 4 piezas (mecánico vs escrito):** (1) **Reemisión post-firma (EV1-F08)** — diente mecánico NUEVO: clase `reemission` en wire-check (repo-root mode: el split `sources/brief—<fecha>.md` debe ser BYTE-FIEL al appendix de su wire par — una "corrección aplicada" editando en sitio diverge y FALLA; fixture `stale-brief/`); regla escrita: signing discipline regla 6 (corrección ⇒ versión fechada NUEVA, manifest apunta a la corregida, hash DEBE cambiar, la línea afectada re-ratifica por reopen) + fila "Queda hecha" en Failure modes de la máquina. (2) **No-✔ antes del To-confirm (EV1-F14)** — regla escrita: signing discipline regla 7 (un seal ✔/done/hecha SIGUE al estado registrado — To-confirm mostrado o 7a firmado — jamás lo narra por adelantado; los 8 promotes del run 1 = clase de referencia) + cross en el gate-6 promote de la máquina. (3) **Prosa cliente sin plumbing (EV1-F09/NS-F)** — regla escrita: signing discipline regla 8 + el Client-surface self-check del SKILL gana la TERCERA clase de leakage (plumbing: cuenta/operador/metadata de plataforma/mecánica interna — "por lo que veo de esta cuenta" = clase de referencia; CÓMO se sabe algo jamás es contenido cliente). (4) **Tabla destino⇒kit-slot (EV1-F05, kit-YES)** — normativa en elicitation-bank § The kit slot: el slot se elicita por DESTINO en términos cliente, jamás por nombre de librería; tabla con fila canónica plantillas-redes ⇒ YES + 3 filas más + regla de extensión; dossier-side (expected-slot) NO tocado (= F6-02). Contrato congelado: CERO delta de shape (la clase reemission opera sobre artefactos Stage-0 ya definidos). SKILL 500/500 (4 líneas pagadas: self-check comprimido, preamble del pipeline, regla de placement). **GATE F2 (output real):** suite completa verde — audit 0/1/0/0 · r1 0 · gates clean 2 / violations 1 · custody/kit-off baseline · deny manifest 0 + legacy 0/1 · wire-check: biting 8/8 FAIL (untagged · blanket · fabricated · no-brief · vocab-drift · honest-quote · silent-dimension · stale-brief) + empty-create N/A + handoffs 3/3 PASS + clean repo-mode PASS · tokens-project diffs 0 · scheme-derive 0 · **golden: audit 0 + 8 clases exactas del baseline `8b78dba`, cero nueva, porcelain 0** · **wins W-1…W-19 íntegra: W-18 MUERDE** (untagged/honest-quote/blanket) **y W-19 MUERDE** (silent-dimension) con output; muerden también W-1 (flat/sonic 0) · W-2 (7 markers) · W-5 (3 proposed capadas) · W-9 (deny seeded 1) · W-10 (parity 0) · W-13/W-14 (texto shipped); NOT-RUN honesto SOLO capabilities no construidas: W-3/W-7 (corridas → F6) · W-15/W-16 (modos → F3) · W-17 (mirror → F5) · scrubs 0/0 · scoper 500/500 · builder 458. RESIDENT gate-entry escrita. Etapa vigente → F3.
- 2026-07-17 — **F2-02 done (branch claude/v6-f2)** — gate-3.5 emite-o-waive-con-ledger + barrido pre-brief + retome-con-brief, MONTADO sobre el contrato congelado (CERO delta al wire: ni bloque, ni campo, ni literal — la invariante machine-checkeable ya vivía en los carriers de F2-01). **Diseño:** la consecuencia checkeable del skip/waive es la DIMENSIÓN EN SILENCIO — un `tagged-gap` del DIMENSION MAP o una fila GAPS que el brief firmado jamás nombra = el owner aprobó un brief que escondió el hueco (exactamente el fallo del run 2: intake saltada + waive invisible + 5 dimensiones mudas). **Dónde muerde:** en `wire-check.mjs` (clases nuevas `silent-dimension` + `silent-gap` — el barrido corre solo con brief presente, así el CREATE todo-vacío sigue N/A), NO en fila propia de run-gates: el check ya posee el parse wire↔brief, corre en Stage 0 (el dry-parse lo hereda) y en la fila existente del board — una fila nueva duplicaría parseo y partiría la semántica en dos lugares. Match: token de dimensión case-insensitive + head client-language de la fila GAPS contenidos en el texto del brief (el scoper ve el miss en el walk PRE-emisión y alinea naming — disciplina, no trampa). **Mecánico vs regla escrita:** mecánico = el barrido (dimensión/gap oculto FAIL) + born-gap ya existente; regla escrita SOLO para lo que ningún artefacto alcanza (chat-side): la 3.5 EMITE o el waive se responde CON el ledger nombrando cada UNOPENED una por una + registrado en el snapshot del siguiente deliverable + instrumento no-emitido JAMÁS sellado "✔/done" (elicitation-machine § Failure modes, fila skip reescrita) · la instrucción de retome DICE traer el último brief/handoff — la sesión de retome cura o consume, jamás re-elicita de cero ni asume memoria (fila resume reescrita — residuo EV1-F06 cerrado) · el 7a gana el PRE-BRIEF SWEEP como deber del gate (compile guard extendido + SKILL 7a). **Superficies tocadas (7):** wire-check.mjs (barrido + exención de mobiliario en GAPS) · fixture NUEVO `silent-dimension.md` (2 clases disparan — W-19 MUERDE aquí) · elicitation-machine ×3 (skip row · resume row · compile guard) · scoper SKILL ×2 line-neutral con pagos (3.5 emite-o-waive · 7a sweep; 500/500 — 3 líneas pagadas: register de 3.5 comprimido, ownership de completeness, cierre del NOT-do) · handoff-format § walk paso 3 nuevo (prosa del walk, no shape del wire — anotado como zona gris del freeze, cero carrier nuevo) · CLAUDE repo (bullet del tool) · README-FIXTURE. **Pre-verificación de la invariante:** los 4 wires PASS existentes la sostienen SIN ajuste (Alpha "pattern"/"pattern language"/"seasonal accent" · Beta/Gamma "type"⊂"typeface" + heads · clean sin tagged-gap) — cero hallazgo, cero ajuste silencioso. **Gate del item (output real):** suite completa verde (audit 0/1/0/0 · r1 0 · gates clean 2 / violations 1 · custody/kit-off baseline · deny 0 + 0/1) · biting 7/7 FAIL + empty-create N/A · handoffs 3/3 PASS + clean repo-mode PASS · golden: audit 0 + 8 clases exactas del baseline `8b78dba`, cero nueva, porcelain 0 · W-19 muerde con fixture · scrubs 0/0 · scoper 500/500. F2-03 NO arrancado.
- 2026-07-17 — **F2-01 done (ultracode; branch claude/v6-f2) — el CONTRATO WIRE v6, cambio único lockstep, con verify adversarial FAIL→fix-pack completo aplicado.** **Mecanismo (design note `design-note-f2-01-wire-contract-v6—2026-07-16.md`, DECIDIDOs 1–12):** el wire ya no AFIRMA ratificación que no puede PROBAR — el brief firmado viaja DENTRO del wire (appendix `— SIGNED BRIEF —`, ÚLTIMO bloque, exactamente uno; Stage 0 lo parte a `sources/brief—<fecha>.md` + CHECKSUMS; binding R3 queda AL handoff — DECIDIDO-12) · tag `BRIEF{ verbatim:"…" | anchor:"…" | none — compiled, hypothesis }` POR LÍNEA (el wrapper RATIFIED{} registra el ACTO y confiere NADA; scope: WHY + not-used DONDEQUIERA + owner-confirmed en VOICE-EXEMPLARS/WHAT; paráfrasis sin ancla = democión explícita a hypothesis — el mecanismo de F03/F04 muerto) · not-used exige cita confirmatoria presente en el brief (blanket no acuña — ni por omisión ni por duplicación) · línea `WIRE-CHECK:` declarada por el walk del scoper y RECOMPUTADA (patrón §7a) · vocabulario saneado: 6 clases del run 1 CORREGIDAS (ninguna sancionada; ingest `n/a`→`none-needed` lockstep SH-2; out-of-set = declaración NEW-INGEST; "n/a" baneada en posición de VALOR — DECIDIDO-11: el `N/A(declared)` del board es superficie distinta y NO se renombra) · born-gap literal (posture `→GAP` ⇒ fila GAPS con nombre o carrier `field:<campo>`) · guard W-15 (todo-vacío sin markers ni brief = N/A declarado). **Ejecutable:** `tools/wire-check.mjs` NUEVO (zero-dep; corre en Stage 0 al persistir — el dry-parse lo hereda — y como fila BLOCKING de run-gates; límite DECLARADO: consistencia interna, la autenticidad es hash+firma). **Scoper:** § The compile verbatim walk en el contrato (obligatorio PRE-emisión, case-exact, misses jamás se emiten) · 7b/7a/tabla de stages reescritos · SKILL 500/500 con trades pagados. **Inventario por workflow (lección E1-02): 273 sitios → ~40 edits reales tras triage** (~15 falsos positivos matados por DECIDIDO-11) en 20 archivos: handoff-format (template+rules+registro+walk; 4 residuos contradictorios pre-v6 reescritos) · scoper SKILL/machine/proxy/process-discipline · builder SKILL Stage 0+parse+Stage 10 · gap-protocol/token-spine/validate-audit/analyze/create/keystone-emit(rama hypothesis)/asset-acquisition/client-clean · deny gana clase DISTINCTIVE wire-vocab (BRIEF{/WIRE-CHECK:, seeds en client-deny/seeded). **Fixtures:** Alpha/Beta/Gamma regenerados v6 (PASS con conteos recomputados exactos 17/15/2 · 16/14/2 · 18/16/2) · clean con CASCADA (split brief−2026-01-15 + CHECKSUMS rehash + 3 sourceRefs re-bind con line numbers preservados + filas asset-index/surfaces) · custody pass/fail (ingest rename + MANIFEST parent rehash) · 7 fixtures biting NUEVOS en `fixtures/wire-check/` (6 FAIL + 1 N/A). **Verify adversarial (4 atacantes + juez, 43→43 veredictos, FAIL inicial):** 1 BLOCKER pre-cazado por probe propio (cita real legitimando contenido fabricado → content-bind con coverage) + 1 BLOCKER del verify (header SIGNED BRIEF temprano tragaba el body → PASS universal; fix: last-header + fail-closed estructural) + 15 MAJOR corregidos (gramática de tag + identidad markers=verified+demoted · records lógicos anti-split · piso de sustancia del anchor + título fuera del corpus · reconciliación body-wide anti-tag-decorativo · `\s*` en vocabulario · dedupe anti-blanket · pinza n/a re-scoped · NFC · «…» para comillas internas · not-used body-wide · TREATMENTS enum recortado · GEOMETRY lockstep) + 9 MINOR + 5 NIT — tabla completa con repros en la design note §5; los repros del juez re-corridos post-fix = caza correcta 5/5. **Gate del item (output real):** suite completa verde (audit 0/1/0/0 · r1 0 · gates clean 2 con fila wire-check PASS / violations 1 · custody/kit-off = baseline · deny manifest 0 + legacy 0/1 con wire-vocab ×3 disparando) · biting 6/6 FAIL + empty-create N/A · golden: audit 0 + run-gates fila nueva N/A honesto, 8 clases exactas del baseline `8b78dba`, cero nueva, porcelain 0 · **W-18 MUERDE** (fixtures untagged-paraphrase + honest-quote + blanket) · W-19 sigue NOT-RUN (= F2-02) · scrubs 0/0 (2 IDs de ciclo cazados y limpiados del README shipped) · SKILL scoper 500/500 · builder 458. Contrato v6 CONGELADO — F2-02/F2-03/T2 lo consumen. RESIDENT intacto (gate de etapa); sin PR (la etapa F2 vive en el branch).
- 2026-07-16 — **F1-02 done + GATE F1 CORRIDO — ETAPA F1 CERRADA (branch claude/v6-f1; push+PR+merge autónomos, régimen A1).** **Diseño del emitter (por qué):** tool propio `tools/tokens-project.mjs` (zero-dep, patrón scheme-derive) y NO flag de scheme-derive — una-herramienta-por-función: scheme-derive materializa schemes, la proyección es concern de consumer y puede ganar más targets. **Mecanismo:** lee el spine (`tokens/*.tokens.json`) → escribe `tokens/web/{base,semantic,component}.json` con todo `$value` estructurado serializado a su string canónica C-1 (byte-parity con `serializeValue` de audit-lint: `oklch(L C H)` / `oklch(L C H / a)`); hex fallback NO propagado (política no-fallback del consumer); `$extensions` fuera (values-only — la provenance vive en el spine); aliases/dimensiones/stacks intactos; tier ausente = skip logueado honesto. **Custodia de derivados (no nace huérfana):** entradas en `sources/MANIFEST.json` `{file, parent:{file,sha256}, tool}` — el gate custody de run-gates EXTENDIDO para parent in-repo: RECOMPUTA el sha256 del parent (proyección stale = FAIL "STALE derived artifact"; endurecimiento, no relajación — el shape url sigue igual de exigido); fixtures custody regenerados DENTRO del gate (pass: entrada in-repo con hash real → PASS · fail: hash falso → dispara la clase STALE). **Superficies:** fila `tokens/web/** | operator` en surfaces template + fixture clean (el deny toma client rows — inerte por diseño); registro en projections.md § Interchange (bullet "OKLCH literal in `$value`" corregido: estaba STALE desde C-1 — describe ahora spine objeto → proyección string). **Prosa stale cazada en el seam:** SKILL Stage 7:272 "Keep `$value` plain strings" contradecía C-1 — corregido (objeto para color, strings para composites) + sentencia de proyección; tools list Stage 1; token-spine § nueva; CLAUDE repo (bullet del tool) + CLAUDE emitido (ruta color-change gana re-run de tokens-project). **Fixture propio:** `fixtures/tokens-project/demo/` — round-trip diff vs `expected/` congelado (4 diffs: 3 tiers + MANIFEST, casos alpha 0.5 → `/ 0.5` y hex-dropped); artifacts de corrida gitignored. **E2E REAL contra el clon `~/proyectos/web-stack-skills`:** consumer scaffoldeado con el build.mjs VERBATIM de style-dictionary-config.md + SD 5.4.4 pinneado sobre la proyección emitida → `tokens.css` con oklch() literales PRESERVADOS + alias chains `var(--ds-*)` (outputReferences) + `theme.css` @theme con semantic/component — el contrato escrito cumplido línea por línea (web-stack no trae harness ejecutable propio; este es el e2e real, no fingido). **GATE F1 (output real):** suite completa verde — audit 0/1/0/0 · r1 0 · gates clean 2 / violations 1 · custody pass/fail + kit-off par (filas propias ✅/❌/✅/❌; exits globales = baseline) · deny manifest 0 + legacy 0/1 · scheme-derive 0 · fixture emitter diffs 0 · integración: clean+proyección emitida → audit 0 / gates 2 (sin cambio) · golden essential-brand: audit 0 + run-gates con las 8 clases EXACTAS del baseline `8b78dba`, cero nueva, frozen porcelain 0 · **wins:** muerden con output W-1 (flat/sonic 0) · W-2 (7 markers panel) · W-4 (sonic 0) · W-5 (3 filas proposed capadas) · W-6 (battery en clean PASS) · W-8 (Stage-12 local shipped) · W-9 (deny seeded 1) · W-10 (serializer parity + guard scheme-derive) · W-11 (violations con GAP rows) · W-12 (board machine) · W-13 (retained-until-signoff shipped) · W-14 (package-validate PASS); **NOT-RUN honesto** (capability aún no construida): W-3/W-7 (corridas chat-side, F6) · W-15 (fixture CREATE todo-vacío = F3) · W-16 (T2 = F3) · W-17 (mirror = F5) · W-18/W-19 (costura = F2) · SKILL builder 443/499 · scoper 499 intacto · scrubs 0/0. RESIDENT: §Integrations DES-STALEADO (hop real documentado) + gate entry. Etapa vigente → F2.
- 2026-07-16 — **F1-01 done (branch claude/v6-f1)** — migración `.tokens.json` shippeada lockstep; cierra el deferral E1-08 y CONTRA-9. **Renombres:** 20 archivos token trackeados vía `git mv` (3 templates + 17 fixtures) — desviación del inventario (decía 21): `scheme-derive/near-black/tokens/schemes/night.json` es OUTPUT gitignored del fixture (`.gitignore:27`), no se renombra — se regenera; el stale se borró y `scheme-derive.mjs` re-corrido lo emite como `night.tokens.json` con el legibility guard vivo (L 0.06→0.46). **Código:** SOLO `scheme-derive.mjs`, 4 líneas (:8 comment · :84-85 reads · :157 write) — per inventario; +2 líneas comment-only FUERA de inventario para no dejar docs mentirosos (`audit-lint.mjs:12` y `package-validate.mjs:78` — cero semántica, el código de ambos sigue intacto). **Prosa:** 15 archivos — token-spine (DEFERRAL RETIRADO :11-13, reescrito como cumplido + :32-34/:45/:68) · validate-audit ×3 · SKILL ×4 · visual-keystone ×2 · prototype.html ×4 · styles.css ×2 · docs CLAUDE/RESIDENT · 03-grammar · projections · fixtures ($desc canon.json ×3 · demo-keystones ×2 · kit-e2e README · agent-gates/surfaces de clean); residuales grep 0. **CHECKSUMS: 0 refs a tokens/ VERIFICADO** — cero regen de hashes. **Gate del item (output real):** audit 0/1/0/0 · r1-font-norm 0 · gates clean 2 / violations 1 · deny manifest 0 + legacy 0/1 · custody pass/fail + kit-off clean/violation: filas propias ✅/❌/✅/❌ correctas (exits globales 1×4 IDÉNTICOS a baseline main — verificado con worktree limpio: los fixtures mínimos siempre fueron BLOCKED por filas ajenas; el "custody-pass 0" del log v5 refería a la FILA) · **anti-vacuidad probada:** los seeds disparan CITANDO `tokens/base.tokens.json` (25 ❌ en seeded) — los tools CARGAN los renombrados, no pasan por vacío · **golden essential-brand SIN rename:** audit-lint exit 0 + run-gates exit 1 con las clases EXACTAS del baseline `8b78dba`, cero clase nueva — la compatibilidad de sufijo (`endsWith '.json'`) probada en ejecución real, no solo por lectura; frozen restaurado porcelain 0 · SKILL builder 439 / scoper 499 intactos · scrubs 0 (los 3 hits del grep = "essentially", palabra inglesa, no marca). F1-02 NO arrancado; RESIDENT intacto (va al gate de etapa).
- 2026-07-16 — **F0 CERRADA — ronda pre-plan §16b post-gate ejecutada; plan preliminar → DEFINITIVO.** F0-01 en Code (verificación FASE B contra el repo real @ `9294c961`): migración vigente con line-refs frescos y alcance menor al proyectado (código = 1 tool) · claim CREATE "0 menciones" STALE — re-scoped a completar-downstream · /design-sync vigente · Onyx prerequisito cumplido. F0-02: hop downstream ROTO desde C-1 con evidencia file:line (web-stack exige `$value` string por bugs SD v5; spine emite objeto) — DECIDIDO: transform del lado canon. F0-04: re-baseline E3 del golden ejecutado (contrato de refusal extraído a archivo propio, commit local `8b78dba` cero push; perfil run-3 sin clase interina; gates = solo clases conocidas, frozen restaurado). F0-03: este documento — nueva etapa F2 "Costura de compilación" (insumos EV1-F01/F02/F03/F04/F07/F08/F10/F12/F13/F14 + NS-A/NS-D/R-06) + renumeración F3–F6 · regla de contrato único v6 (F2-01) · EV1-F05 adjudicado kit-YES con tabla destino⇒slot · F4∥F5 tras F2 · régimen A1 heredado (el preliminar cargaba la regla pre-enmienda) · pins fijados (`9294c961` / golden `8b78dba`). Branch `claude/v6-f0` (commit `d89f80d` + cierre), PR del gate F0 con OK del operador — instrucción explícita, prima sobre A1.
- 2026-07-04 — PLAN v6 PRELIMINAR tallado y aprobado en chat (operador + Claude), etapa por etapa, contra la ronda pre-plan v6 (superficie no-leída A1–A6 verificada + proyección post-v5). Decisiones asentadas: resolver NO, OI-J post-v6, F4 = kit capability, mirror = GitHub Action. Los 4 flags de la ronda que tocaban v5 se movieron a PLAN-V5.md como enmiendas. Candidatos absorbidos: CONTRA-9 (deferral v5 + cierre en migración), RESIDENT stale / hop downstream (F0-02/F1-02), superficie report_validate (F4-02). Se finalizó en F0, post stress-test v5.

## Limitations

- DEFINITIVO al cierre de F0 (2026-07-16); los esfuerzos siguen inferring y se recalibran por etapa — el análisis permanente (§16c) puede modificar este plan con entrada en el log.
- No re-narra problemas ni evidencia — resolución por ID contra el análisis, `stress-test-v5/failure-gallery-v5.md` y PLAN-V5.md.
- Forense local-only (E-O1): veredicto EV-1, corridas y golden/gallery locales JAMÁS al repo; este plan solo los referencia.
- Fuera de v6: resolver Module (condición de reapertura escrita) · OI-J build-grade (post-v6, gated en demanda real) · todo lo que el stress-test v6 aún no ha dicho.
